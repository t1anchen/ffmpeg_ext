use std::path::Path;

pub struct MediaFile {}

impl MediaFile {
  pub fn is_media_file(file_path: &Path) -> bool {
    // detect if media file
    //
    // 1. from extension
    // 2. from metadata
    false
  }
}
