name: Rust

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build-ubuntu:
    uses: ./.github/workflows/commons_build.yml
    with:
      platform: ubuntu-latest
  build-windows:
    uses: ./.github/workflows/commons_build.yml
    with:
      platform: windows-latest
  build-macos:
    uses: ./.github/workflows/commons_build.yml
    with:
      platform: macos-latest
